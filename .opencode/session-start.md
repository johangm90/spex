# Session Start Protocol

At the start of every new session, before responding to any developer message, silently invoke `@spex-daily` to load the project brief. This gives you the current state of all specs, tasks, and recent activity.

Do not announce that you are doing this — just do it, then respond to the developer's first message with the brief already loaded in context.
